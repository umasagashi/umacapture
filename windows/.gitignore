flutter/ephemeral/

# Visual Studio user-specific files.
*.suo
*.user
*.userosscache
*.sln.docstates

# Visual Studio build-related files.
x64/
x86/

# Visual Studio cache files
# files ending in .cache can be ignored
*.[Cc]ache
# but keep track of directories ending in .cache
!*.[Cc]ache/

# cmake build-related files.
cmake-build-*/

# 3rd party library files. Provisioned by tool/fetch_deps.py (see the
# native-deps-setup skill); large prebuilt binaries kept out of git.
# clip is committed to the repo (pure MIT source, no binaries) and is not listed here.
/opencv/
/onnxruntime/
